# Copy this file to `.env` and set OBSIDIAN_VAULT_PATH to the absolute path
# of the Obsidian vault you want `pnpm run dev` and `pnpm run build:local` to
# install into. `.env` is gitignored.
OBSIDIAN_VAULT_PATH=/absolute/path/to/your/vault
